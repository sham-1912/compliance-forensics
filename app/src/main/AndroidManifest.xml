<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMISSIONS -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG"
        tools:ignore="PhonePermissionUsage" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComplianceForensicsEngine"
        tools:targetApi="31">

        <!-- Real-Time Call Verification Screen (Launcher) -->
        <activity
            android:name="com.compliance.forensics.ui.CallVerificationActivity"
            android:exported="true"
            android:theme="@style/Theme.ComplianceForensicsEngine">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Audit Log Viewer Dashboard -->
        <activity
            android:name="com.compliance.forensics.ui.LogsActivity"
            android:exported="false"
            android:theme="@style/Theme.ComplianceForensicsEngine" />

        <!-- Broadcast Receiver for Call Detection -->
        <receiver
            android:name="com.compliance.forensics.CallReceiver"
            android:exported="true"
            android:permission="android.permission.READ_PHONE_STATE">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Audit Foreground Service -->
        <service
            android:name="com.compliance.forensics.service.CallListenerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="phoneCall"
            tools:ignore="ForegroundServiceType" />

    </application>

</manifest>